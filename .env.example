# Copy this file to .env before starting Docker Compose, then replace these values.
# Generate JWT_SECRET with a cryptographically secure base64 value (at least 32 bytes).
MYSQL_ROOT_PASSWORD=replace-with-a-strong-mysql-root-password
JWT_SECRET=replace-with-a-strong-base64-jwt-secret
